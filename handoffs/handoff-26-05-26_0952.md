# Handoff — 26/05/26, 09:52

**Session ended:** 2026-05-26 at 09:52 local time
**Branch:** `master` (2 commits ahead of `origin/master`, unpushed)
**Working dir:** `C:\Users\joshc\OneDrive\Documents\Project`

> Filename note: the skill spec format `handoff-DD/MM/YY,HH:MM` uses characters reserved on Windows (`/` and `:`). This file uses the closest legal equivalent `handoff-DD-MM-YY_HHMM.md`. Update the skill if you want a different convention.

---

## TL;DR

Closed out the monthly Betfair download automation properly: backfilled April 2026, surfaced and fixed two bugs the previous session left silent (split scripts crashing on missing `_pre` columns; monthly script lying about success), updated the Obsidian vault to current reality, and built a new `/sync-vault` skill so the vault stays current going forward. Two new pre-live hardening findings logged.

---

## What was done this session

Full detail lives in artifacts — referencing rather than duplicating:

- **Code commits on `master`** (both unpushed):
  - `e27ef0a` `feat: monthly Betfair historical data automation`
  - `1264249` `fix: handle missing _pre columns gracefully in league-split scripts`
- **Memory update:** `~/.claude/projects/C--Users-joshc-OneDrive-Documents-Project/memory/pre_live_hardening.md` items 6 and 7 added
- **Vault updates** (3 notes bumped to `updated: 2026-05-26`):
  - `C:\Users\joshc\OneDrive\Desktop\Vault\Projects\Bet Bot\BetBot_index.md` — Current status reflects PL season ended + monthly Betfair download automated; Open Questions section has new findings
  - `C:\Users\joshc\OneDrive\Desktop\Vault\Projects\Bet Bot\data\sources.md` — removed false "broken since April 6" claim, added cert-auth/API path
  - `C:\Users\joshc\OneDrive\Desktop\Vault\Projects\Bet Bot\progress.md` — new dated entry "26 May 2026: Monthly Betfair automation + split-script hardening"
- **New project skill:** `.claude/skills/sync-vault/SKILL.md` — encodes which kinds of changes map to which vault notes. **Caveat:** lives under `.claude/` which is gitignored — won't survive a clone (see Decisions Pending below).
- **Validation:** Monthly Betfair scheduled task re-triggered after fixes — `LastTaskResult=0` confirmed; +29 PL BTTS split-file rows regenerated cleanly.

---

## What's pending

Tasks tracked in this session's task list (`TaskList` will show them):

| Task | Priority | Notes |
|---|---|---|
| `#8` Investigate stale EFL canonical CSV | **High** | `CompleteDSChamp_CSV.csv` stops at 2025-05-03; entire 2025-26 Championship season missing. All April 2026 EFL Betfair rows silently dropped on the canonical join. **Blocker for any EFL live betting.** PL canonical is fine. |

Beyond the task list:

| Item | Priority | Where it's tracked |
|---|---|---|
| Re-extract historical Betfair months to backfill `*_ltp_pre` columns | High | `pre_live_hardening.md` #6; `BetBot_index.md` Open Questions |
| Original 5-item pre-live hardening punch list (settlement parser, save_recs UPSERT, zero-div best odds, stale cache TTL, settle path conflict) | Medium | `pre_live_hardening.md` items 1–5; still untouched |
| Fix `Bettig`→`Betting` typo on Daily Settlement scheduled task | Low | **Needs admin PowerShell** — see snippet below |
| Decide whether to track `.claude/` in git | Meta | See "Decisions Pending" |
| Push the 2 unpushed commits to `origin/master` | Trivial | `git push` |

### Admin PowerShell one-liner for the typo fix

```powershell
$xml = schtasks /Query /TN "Bettig Bot Daily Settlement" /XML | Out-String; Unregister-ScheduledTask -TaskName "Bettig Bot Daily Settlement" -Confirm:$false; Register-ScheduledTask -TaskName "Betting Bot Daily Settlement" -Xml $xml
```

---

## Decisions pending

1. **`.claude/` gitignore status.** Currently `.claude/` is in `.gitignore` (line 47), but the project CLAUDE.md says it should be checked in for project-local agents and skills. Three options were discussed:
   - Targeted ignore with allowlist for `agents/`, `skills/`, `commands/`
   - Remove `.claude/` from `.gitignore` entirely
   - Accept that project skills are local-only
   Until resolved, the new `sync-vault` skill is on this machine only.

2. **Handoff file naming convention.** Updated skill format `handoff-DD/MM/YY,HH:MM` is not Windows-legal (see top of doc). Worth deciding final canonical format and editing the global skill once.

3. **Other uncommitted changes from a prior session** are sitting in the working tree but were deliberately not touched this session:
   - `M backtest.py` (+63 / -7)
   - `M model.py` (+10 / -4)
   - `M predict.py` (+7 / -7)
   - `?? CONTEXT.md` (221 lines, new)
   - `?? scripts/lr_ablation_test.py` (522 lines, new)
   - `?? rsndom.py` (0 bytes — almost certainly accidental)

   `model.py` and `predict.py` are "DO NOT TOUCH without approval" files per the project CLAUDE.md, so they need explicit decision before commit.

---

## Suggested first moves for next session

1. **`TaskList`** to see task #8 still pending.
2. **Read `pre_live_hardening.md`** and `BetBot_index.md` "Open questions" — both reflect current truth as of end of this session.
3. **Decide priority:** either (a) dive into the EFL canonical CSV investigation (task #8), (b) address the `.claude/` gitignore decision so future skills survive, or (c) take on items 1–5 of the original pre-live hardening list.
4. **Push unpushed commits when ready** (`git push` — no force, no special flags).
5. **End the next session with `/sync-vault`** to keep the vault current before the next handoff.

---

## Suggested skills

| Skill | When | Why |
|---|---|---|
| `/sync-vault` (new, project-scoped) | End of next session, before `/handoff` | Encodes the discrimination map from changes to vault notes. Prevents the drift this session demonstrated. |
| `/diagnose` or `/gstack-investigate` | If task #8 (EFL canonical) reveals broader data-pipeline issues | The investigation might surface that `championship_pipeline.py` has been training on stale data — root-cause discipline matters. |
| `/grill-with-docs` | Before any larger change (e.g. starting on the pre-live hardening punch list, re-extracting historical Betfair data) | These are money-adjacent decisions; the project rules require it. |
| `/gstack-careful` or `/gstack-guard` | If touching `config.py`, `predict.py`, or any live-betting module | Project rule for money-adjacent code. |
| `/tdd` | If modifying any logic in `predict.py`, `staking.py`, edge calculations | Project rule: failing test first for money-adjacent logic. |

---

## References

- **Commits:** `git log --oneline origin/master..HEAD` (shows the 2 commits this session)
- **Project CLAUDE.md:** `C:\Users\joshc\OneDrive\Documents\Project\CLAUDE.md`
- **Global CLAUDE.md:** `C:\Users\joshc\.claude\CLAUDE.md`
- **Memory directory:** `C:\Users\joshc\.claude\projects\C--Users-joshc-OneDrive-Documents-Project\memory\`
- **Pre-live hardening memo:** `~/.claude/projects/C--Users-joshc-OneDrive-Documents-Project/memory/pre_live_hardening.md`
- **Vault root:** `C:\Users\joshc\OneDrive\Desktop\Vault\Projects\Bet Bot\`
- **Scripts touched this session:** `scripts/betfair_monthly_update.py`, `scripts/monthly_betfair_update.bat`, `scripts/extract_btts_by_league.py`, `scripts/extract_efl_ou15_betfair.py`, `scripts/download_betfair_goals.py`, `config.py`, `.gitignore`
- **New skill:** `.claude/skills/sync-vault/SKILL.md`
- **Scheduled tasks:** `Betting Bot Monthly Betfair Update` (next run: 5 June 2026 10:00 — will hit off-season skip), `Betting Bot Weekly Retrain`, `Bettig Bot Daily Settlement` (typo'd name)
- **Previous handoff (different format, in Temp):** `C:\Users\joshc\AppData\Local\Temp\betbot_handoff_2026-05-20.md`
