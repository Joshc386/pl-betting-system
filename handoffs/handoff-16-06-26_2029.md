# Handoff — 16/06/26, 20:29

**Session ended:** 2026-06-16 at 20:29 local time
**Branch:** `master` (in sync with `origin/master` — everything pushed)
**Working dir:** `C:\Users\joshc\OneDrive\Documents\Project`

> Filename uses the Windows-legal convention `handoff-DD-MM-YY_HHMM.md` (the skill's `DD/MM/YY,HH:MM` spec contains characters illegal on Windows). This is a **new, immutable block** in the handoff chain — it references the previous handoff but does not edit it (see [[feedback-handoff-immutability]]).

**Previous block:** `handoffs/handoff-26-05-26_0952.md`

---

## TL;DR

Worked a 3-phase plan. **Phase 1** (track project-local `.claude/`) and **Phase 3 items #2 + #3** (UPSERT recs, zero-div odds) are done, committed, pushed. **Phase 2** (EFL canonical refresh) is **paused mid-flight**: the data layer is regenerated and committed, but a domain rule the user surfaced — previous-season league-position seeding (ADR-0002) — must land *before* the EFL retrain, so retraining is deferred. Also recovered from a non-trivial discovery: the broken DB tests had been writing fake rows into the production PL DB.

---

## What was done this session

All detail lives in commits/ADRs/memory — referencing, not duplicating.

**Commits (all pushed, `e0a4bf1..27e0b33`):**
- `d970586` Phase 1 — track `.claude/` agents/commands/skills via gitignore allowlist
- `62bfb99` + `26efffa` Phase 2 data layer — 2025/26 EFL season added, cache-path `dropna` bug fixed, Wrexham Betfair mapping; CONTEXT.md terms + ADR-0001 + ADR-0002
- `7a2e6c9` Phase 3 item #2 — `save_recommendations` UPSERT on re-scan + fixed test DB isolation
- `0df3f37` + `27e0b33` Phase 3 item #3 — zero-priced-book crash fixed in `get_best_odds` **and** the BTTS sibling

**Also (not in git):**
- Earlier in session: LR-as-4th-stacker work, stacker contract tests, handoff-chain tracking (commits before `d970586`, already pushed last session start)
- **Deleted 3 production-pollution rows** from `data/dashboard.db` (Arsenal/Chelsea, Liverpool/Chelsea — written by the previously-broken tests). DB is gitignored; this was a live mutation, already done and verified.
- New memory: `feedback_handoff_immutability.md`; updated `MEMORY.md` header (PL=4-model, EFL=3-model)

**Key decisions captured as ADRs (read these):**
- `docs/adr/0001-canonical-dataset-regeneration-criteria.md` — facts byte-identical, computed features may drift, models retrain on current-script output
- `docs/adr/0002-league-position-previous-season-seeding.md` — **the gating task for the EFL retrain** (see below)

---

## RESUME HERE — Phase 3 item #4 (stale-cache TTL + retry)

The session ended right after presenting the item #4 plan, **awaiting approval to code**. The plan (money-adjacent → use `/tdd`):

**4A — Cap stale cache at 90 min.** Add `STALE_TTL_MINUTES = 90` in `api/odds_api.py`; restructure `_load_cache` ([api/odds_api.py:35](api/odds_api.py:35)) so: <30 min served normally; 30–90 min served only as API-failure fallback; **>90 min never served** (return `None` → caller gets `[]`, never bets on ancient odds). Fixes the unbounded `allow_stale` at [api/odds_api.py:50](api/odds_api.py:50) and the fallback at [api/odds_api.py:433](api/odds_api.py:433).

**4B — Retry the bulk totals fetch.** Wrap `_fetch_market` ([api/odds_api.py:73](api/odds_api.py:73)) in the OddsPapi retry pattern ([api/oddspapi.py:147](api/oddspapi.py:147) is the template): `max_retries=2`, `wait=2**attempt`, retry only on `status>=500` and `requests.Timeout`, never on 429/4xx. Keep the quota guardrail BEFORE the loop; mock `time.sleep` in tests.

**Open question the user has NOT answered:** whether to do both 4A+4B or just 4A this session, and confirm the 90-min cap. Default recommendation: both, via TDD, 90-min cap.

**Then item #5** (per user's ordering): settle-path conflict + quota filelock — `scheduler.py` / `settlement.py` / `api/quota_tracker.py`. Three concurrent settle paths can double-count bankroll; quota tracker is unlocked read-modify-write (add `filelock`). This is the hardest (concurrency); see `pre_live_hardening.md` item 5.

---

## Phase 2 interim state (important — do not misread)

- `CompleteDSChamp_CSV.csv` on disk = **regenerated** (2025/26 included, facts verified, but matchday-1 league position still computed the old/imperfect way).
- EFL models in `models/championship/` = **OLD, untouched** (still the live ones, pre-2025/26). **The EFL retrain was deliberately NOT run.**
- This is safe: EFL is off-season (0 live recs, no fixtures until Aug 2026).
- **Phase 2 completion (stages 2e–2g: rebuild cache → retrain → verify Betfair join) is BLOCKED on ADR-0002** — do the seeding first, then retrain once on corrected data. Don't retrain on the current imperfect feature.

---

## Other pending / deferred

| Item | Status | Where |
|---|---|---|
| ADR-0002 league-position seeding | Spec'd, not implemented. Cross-league (PL+EFL build scripts), needs promotion-route data for ~25 historical seasons, TDD. Gates the EFL retrain. | `docs/adr/0002-*.md` |
| Phase 3 item #1 (alt-line settlement parser `ou175`/`ou20`/etc) | **Deferred by user** until system fully complete — a bolt-on, not live yet | `pre_live_hardening.md` #1; [settlement.py:72](settlement.py:72) |
| `.bak` backups | Retained per plan (canonical + 8 EFL pickles). Clean up after the eventual EFL retrain is verified. `CompleteDSChamp_CSV.csv.bak` is untracked at repo root (not gitignored — don't `git add -A`). | repo root + `models/championship/` |
| Enriched CSV missing | `league_config.py:99` refs `CompleteDSChamp_enriched.csv` which doesn't exist on disk. Flagged, not chased — confirm pipeline falls back gracefully. | — |
| Pre-scan spec drift | `.claude/skills/pre-scan/` expects EFL pickles at `models/` but they're in `models/championship/`. Minor doc fix. | — |

---

## Suggested skills

| Skill | When | Why |
|---|---|---|
| `/tdd` | Resuming item #4 (and #5) | Money-adjacent; failing test first per project rules. Item #4 4A/4B test designs are in the plan above. |
| `/grill-with-docs` | Before starting ADR-0002 seeding implementation | Cross-league money-adjacent feature; needs design + the promotion-route data model nailed down. ADR-0002 is the spec; grill the implementation approach. |
| `/pre-scan` | Start of any session touching data pipeline / scheduler / settlement | Project rule. Note the spec-drift fix above. |
| `/sync-vault` | End of next session before `/handoff` | Keep the Obsidian vault current (project rule from previous handoff). |

---

## References

- **Unpushed commits:** none — all pushed to `origin/master`
- **This session's commits:** `git log --oneline e0a4bf1..27e0b33`
- **Punch list (source of truth for items 1–5):** `~/.claude/projects/C--Users-joshc-OneDrive-Documents-Project/memory/pre_live_hardening.md`
- **Domain language:** `CONTEXT.md` (added: Canonical Dataset, Data Refresh, League Position seeding)
- **ADRs:** `docs/adr/0001-*.md`, `docs/adr/0002-*.md`
- **Memory dir:** `C:\Users\joshc\.claude\projects\C--Users-joshc-OneDrive-Documents-Project\memory\`
- **Previous handoff:** `handoffs/handoff-26-05-26_0952.md`
