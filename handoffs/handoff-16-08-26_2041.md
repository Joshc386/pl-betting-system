# Handoff — 16/08/26, 20:41

**Session ended:** 2026-08-16 at 20:41 local time (Sunday)
**Branch:** `feat/agreement-from-oof-cache` (unchanged — **nothing was committed**)
**Commits this session:** 0
**Working dir:** `C:\Users\joshc\OneDrive\Documents\Project`

> New **immutable** block in the chain — references the previous handoff, never edits it.

**Previous block:** `handoffs/handoff-16-08-26_1559.md`

**Next session focus:** commit this session's work (one decision needed — see
"RESUME HERE"), then the previous block's still-open items.

---

## RESUME HERE — one decision, then commit

**Nothing is committed.** The work is complete, tested and documented, but the
tree is dirty and now holds **three** sessions' uncommitted work.

The decision: `CONTEXT.md` carries this session's **Division Movement Seed**
vocabulary *and* a prior session's **column coverage** vocabulary, and the
latter pairs with their uncommitted `data/build_canonical_dataset.py`.
Committing it whole ships their doc without their code. `git add -p` is
unavailable in this environment, so there is no clean split without
hand-building a staged blob — the same bind the previous block hit with
`dashboard.py`.

**This session's files, unambiguously:**

```
new:      division_movement.py
          tests/test_division_movement.py
          docs/adr/0011-one-division-movement-seed-per-arrival.md
          scripts/measure_seed_weight.py
          scripts/validate_seed.py
modified: championship_pipeline.py  championship_predict.py  model.py
```

**Not this session's, untouched:** `db.py`, `scan.py`, `dashboard.py`,
`staking.py`, `job_health.py`, `api/team_mapping.py`,
`data/build_canonical_dataset.py`, the four backtest runners,
`tests/test_canonical_builder.py`, `tests/test_job_health.py`,
`tests/test_model_agreement.py`, and the two DB backups.

The user was asked whether to commit the eight files and leave `CONTEXT.md`,
or hold entirely. **They chose neither — the session ended first.** Ask again
before committing.

---

## What was built

[ADR 0011](docs/adr/0011-one-division-movement-seed-per-arrival.md) and the
**Division Movement Seed** entry in `CONTEXT.md` carry the full reasoning,
every measurement, and the four rejected alternatives. Not repeated here.

**830 tests pass**, 15 covering this work.

Two production defects are fixed and verified against live artefacts:

1. **Live path** (`_synthesize_promoted_fixture`) no longer hands an arriving
   side a league-median feature row.
2. **Dixon-Coles** no longer rates an arrival on its exit season.

---

## What the previous block asked, and what came back

Its "RESUME HERE" said the promoted-team mechanism already existed and that
the Wolves finding must be re-scoped against it before acting. **It did exist,
and three of that section's claims did not survive checking:**

- **Rolling form is handled.** `initialize_promoted_features` exists in both
  pipelines, and `latest_df` is scoped to the current season, so a returning
  side's ancient rows are unreachable by construction. `_detect_new_teams`
  catches all 109 historical gap-returners.
- **Lincoln is not a defect.** Handled at three layers — resolution, feature
  row, and DC's `PRIORS`. Verified against the real trained model.
- **The `RuntimeWarning` at `championship_pipeline.py:480` is not Lincoln's.**
  It is 51 rows in seasons 0–14 with both Poisson inputs NaN. Lincoln
  contributes zero rows to 14,160, so it cannot be the cause.

**The instinct was right, the location was wrong.** Stale form does reach
predictions — through **Dixon-Coles**, not the rolling features, because
`_decay_weights` decays by position in a team's own match sequence rather than
by date.

---

## Judgement calls a fresh agent should not silently reopen

- **Criterion 4 was honoured; the PL-form transfer is dropped.** `w = 0.317`,
  CI `[-0.22, 0.85]`. Do not re-propose it without new events —
  `scripts/measure_seed_weight.py` re-runs the measurement; three accrue per
  season.
- **Criterion 1 failed as written and it shipped anyway.** +1.35% log-loss,
  CI spanning zero. The reasoning is in ADR 0011's **Status**: criterion 1
  gates a defect fix, where the replaced state is demonstrably wrong, not a
  speculative addition. The user said "Ship it" to that recommendation.
- **`seed_features` takes a `params` argument it never uses.** The priors turned
  out to belong to DC's fit. Flagged to the user, deliberately left rather than
  changed unilaterally — it was in the approved interface.
- **`backtest_promoted.py` was the wrong harness** for criterion 1 and was not
  run. It measures whether seeding training rows beats not seeding them; this
  work relocated that logic rather than changing it.

---

## Deployment

**Blocked, by design, and nothing here reaches live recommendations yet.** The
seed changes training rows *and* serving rows, so it takes effect on the next
EFL retrain — which the Freshness Gate still refuses because upstream has not
published 2026/27. The unblocking sequence in the previous block is unchanged.

A pre-ADR-0011 pickle has no `seed_params` and falls back to the single
hand-picked `PRIORS` bucket rather than failing, so the current trained state
is safe to keep using until the retrain.

---

## Still open (carried forward)

Everything in the previous block's "Still open" survives untouched — Phase 3
visual overhaul, the two dashboard display bugs, `api/football_data.py` as dead
code, the `.claude/agents/` drift review, `edge_analytics.py:601`'s double
backtest, Freshness Gate observability, the two DB backups, and the five
unregenerated OOF cells.

Added this session:

- **PL promoted clubs still get no recommendations** until they have a
  current-season row — `predict.py:983` skips the fixture. Roughly 3 fixtures a
  season (Coventry, Hull, Ipswich for 2026/27). Deliberate and conservative;
  ADR 0011 records it as out of scope.
- **The PL never reads the Championship data it holds.** Those three clubs each
  have 46 rows in the EFL canonical's latest season and `pipeline.py` has no
  cross-league read at all. This is the real prize the user's own rationale
  points at, and it is a project, not a patch.
- **Pre-scan warnings** (2026-08-16): OddsPapi caches 151h old — a symptom of
  the gate blocking scans, not an independent fault; and **Portsmouth v QPR has
  no O/U market at all** (zero odds-api bookmakers, no OddsPapi entry, one BTTS
  book). Model pickles were 6.3 days at 20:41 and the EFL retrain is gated, so
  EFL crosses the 7-day staleness threshold imminently.

---

## Suggested skills

| When | Skill |
|---|---|
| **Start of next session** — touches the data pipeline | `/pre-scan` |
| Before committing three sessions' overlapping work | `/gstack-review` |
| Cross-model check on the seed diff — it prices bets | `/gstack-codex` |
| If the PL cross-league seeding project is taken up | `/grill-with-docs` then `/tdd` |
| If a rebuild misbehaves once 2026/27 lands | `/diagnose` |
| End of next session | `/handoff` |

**Do not** reach for an agent team. Single-threaded work, and files two other
sessions may still be holding.

---

## User preferences reinforced this session

- **Plan before code, without exception.** Seven grilling questions were
  answered one at a time before a line was written, and the TDD build began
  only after the interface and all twelve behaviours were approved.
- **Pre-commit the decision rule, then honour it.** Criteria were agreed before
  the numbers were seen precisely so they could not be reinterpreted
  afterwards. When criterion 4 failed, the user was told plainly and the
  mechanism was dropped.
- **Measure, never assert.** Every constant in this work is measured. The user
  chose the fitted option at every fork — "fit the weight, walk-forward",
  "one global w", "measure both routes".
- **Correct plainly and move on.** Three claims inherited from the previous
  block were refuted, and one of this session's own recommendations ("the one
  real defect") was withdrawn. The retraction is valued over the confident
  answer.
- **The user knows this codebase.** They supplied the PL/EFL asymmetry
  rationale unprompted and it reframed the whole design; they also specified
  the squad-decay adjustment that became `w`. Check their recollection against
  the code rather than dismissing or assuming it.
